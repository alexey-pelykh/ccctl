{
  "name": "@ccctl/web-ui",
  "version": "0.0.0",
  "description": "ccctl zero-build static UI: plain HTML + vanilla ES modules (EventSource + fetch), no framework, no bundler",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.19.0"
  },
  "license": "AGPL-3.0-only",
  "author": "Alexey Pelykh (https://github.com/alexey-pelykh)",
  "files": [
    "index.html",
    "src"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "build": "rm -rf dist && mkdir -p dist && cp index.html dist/ && cp -r src dist/ && find dist/src -name '*.test.js' -delete",
    "typecheck": "node --check src/app.js && node --check src/transcript.js && node --check src/command.js && node --check src/sessions.js",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint src/",
    "dev": "echo \"serve this directory statically, e.g. npx serve .\""
  },
  "devDependencies": {
    "vitest": "^4.1.9"
  }
}
